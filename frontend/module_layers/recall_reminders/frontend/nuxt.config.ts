// Nuxt layer for the `recall_reminders` module. Pure backend glue — no
// pages or i18n of its own, but every module needs a frontend layer
// folder per the established convention (none of the 22 upstream
// modules omit this key).
export default defineNuxtConfig({})
